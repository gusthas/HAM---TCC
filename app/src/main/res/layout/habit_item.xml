<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:padding="12dp"
    android:background="@drawable/rounded_semi_transparent"
    android:gravity="center_vertical"
    android:layout_marginVertical="4dp">

    <ImageView
        android:id="@+id/icone_habito"
        android:layout_width="32dp"
        android:layout_height="32dp"
        android:layout_marginEnd="12dp"
        app:tint="@android:color/white"
        android:contentDescription="Ícone do Hábito"
        tools:src="@drawable/ic_habits" />

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical">

        <TextView
            android:id="@+id/habit_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@android:color/white"
            android:textSize="16sp"
            android:textStyle="bold"
            tools:text="Ler 10 páginas" />

        <TextView
            android:id="@+id/text_streak_days"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="#CCCCCC"
            android:textSize="13sp"
            tools:text="5 dias seguidos" />

        <TextView
            android:id="@+id/text_streak_message"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="#CCCCCC"
            android:textSize="12sp"
            android:textStyle="italic"
            tools:text="Continue firme!" />
    </LinearLayout>

    <TextView
        android:id="@+id/habit_count"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        tools:text="1"
        android:textColor="@android:color/white"
        android:textSize="16sp"
        android:textStyle="bold"
        android:layout_marginEnd="16dp" />
    <ImageButton
        android:id="@+id/btn_favorite"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:src="@drawable/ic_star_outline"
        app:tint="#FFD700"
        android:contentDescription="Favoritar Hábito" />

    <ImageButton
        android:id="@+id/btn_check_done"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:src="@drawable/ic_remove"
        android:scaleType="centerInside"
        android:contentDescription="Marcar como desfeito"
        android:layout_marginStart="8dp"
        app:tint="#FFFFFF" />

    <ImageButton
        android:id="@+id/btn_check"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:src="@drawable/ic_check_habit"
        android:scaleType="centerInside"
        android:contentDescription="Marcar como feito"
        app:tint="#FFFFFF" />

</LinearLayout>